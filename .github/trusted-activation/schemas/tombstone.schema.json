{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/NTHU-Physics-SA-IT/PastExamWeb_PHY/.github/trusted-activation/schemas/tombstone.schema.json",
  "title": "Trusted Activation Retired Name Tombstone",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "record_type", "repository_id", "repository", "branch", "activation_id", "reason", "retired_at"],
  "properties": {
    "schema_version": {"const": 1},
    "record_type": {"const": "tombstone"},
    "repository_id": {"type": "integer", "minimum": 1},
    "repository": {"const": "NTHU-Physics-SA-IT/PastExamWeb_PHY"},
    "branch": {"type": "string", "pattern": "^integration/[A-Za-z0-9][A-Za-z0-9._-]*$"},
    "activation_id": {"type": "string", "format": "uuid"},
    "reason": {"type": "string", "minLength": 1, "maxLength": 240},
    "retired_at": {"type": "string", "format": "date-time"}
  }
}
